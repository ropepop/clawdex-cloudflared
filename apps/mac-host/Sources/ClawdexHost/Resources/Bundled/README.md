This directory holds the bundled `codex-rust-bridge` binary for the macOS host app.

Run `npm run build -w @codex/mac-host` or `npm run dev -w @codex/mac-host` to build the
Rust bridge and copy the executable here before launching the app.
